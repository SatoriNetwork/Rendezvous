'''
a client is a peer (it's only a client in relation to the server). each peer
must connect to the server before it can connect to other peers. so each peer is
also a client. this code is called by the peer.
'''
